<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fire.auth.com.raddi_new">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- android:theme="@style/AppTheme" -->


        <!-- <activity android:name="com.theartofdev.edmodo.cropper.CropImageActivity" -->
        <!-- android:theme="@style/Base.Theme.AppCompat"/> -->
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CreatePage" />
        <activity android:name=".Home" />
        <activity android:name=".PostBook" />
        <activity
            android:name=".Profile"
            android:parentActivityName=".Home" />
        <activity
            android:name=".PostPage"
            android:parentActivityName=".Home" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".UserProfileShow" />
        <activity android:name=".Feedback" />
        <activity android:name=".UserAllPost" />
        <activity android:name=".Wishlist" />
        <activity android:name=".AboutUs" />
        <activity android:name=".ProfileActivityNew" />
        <!-- <activity android:name=".Profile_2"></activity> -->

        <activity android:name=".ProfileEdits" />
        <activity android:name=".RequestPageSection"></activity>
    </application>

</manifest>